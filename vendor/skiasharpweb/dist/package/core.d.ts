/** Legacy entry: caller supplies CanvasKit in Node; browser defaults load gallery fonts. */
export { Initialize, RegisterWebComponent } from './index.js';
export { Initialize as default } from './index.js';
