# Avalonia.OpenGL browser adapter

WebGL2 callback lifecycle, scoped context leases, loss/restoration and native Skia RGBA readback. Desktop OpenGL pointers, cross-context native textures and zero-copy interop are not exposed.
