# Verified source import

Staging the complete AvaloniaWeb 0.6.2-alpha.1 framework, compiler, ControlCatalog and test sources for this repository. The import verifies content hashes, restores the pinned local rendering/reactive dependencies, rebuilds generated assets, runs the tests, and commits the ordinary source files. Full-isolation is the catalog default. These staging files are removed after the import succeeds.
